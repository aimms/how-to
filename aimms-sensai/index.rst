.. image:: /Images/SENSAI.png
   :scale: 130
   :align: right

Sensai
========================

.. meta::
   :keywords: SENSAI, AI assistant, AIMMS, optimization, natural language, decision support, ask the model
   :description: SENSAI is the AIMMS family of AI assistants. Each one works inside an optimization application and answers from that application's own model, so the numbers in an answer are the numbers the application produces.


SENSAI is the AIMMS family of AI assistants. Each one works inside an optimization application and
answers from that application's own model: the data it holds and the procedures it runs. The numbers in
an answer are the numbers the application itself produces, reached by asking rather than by clicking
through a screen.

The assistants differ in where they live and who talks to them. Find the one you are working with below:

.. toctree::
   :maxdepth: 1
   :titlesonly:

   sensai-apps/index
