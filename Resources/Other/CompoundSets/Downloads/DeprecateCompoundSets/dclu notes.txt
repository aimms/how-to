! Fill in the element parameters that map the tags.

! Declare element parameters that are to be used as a substitution for tags.
ElementParameter epTag_C_TT {
    IndexDomain: iMS_C;
    Range: T;
}
definition: first( j | exists( i, k ) | ( i, j, k, ims_c ) in sSetMappingRelation_C )
needed: epComponentIndex( iCompoundSet, iIDP )
have:
sSetMappingRelation_C
ElementParameter epIndexDomainSets {
    IndexDomain: (IndexIdentifiers,iIDP);
    Range: AllSets;
}
